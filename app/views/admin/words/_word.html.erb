<%= will_paginate %>
<% words.each do |word| %>
	<li>
	  <%= link_to word.content_jp, admin_word_path(word) %>
	    | <%= link_to t(:delete), admin_word_path(word), method: :delete, 
	                   			data: { confirm: t(:you_sure) } %>
	  <% if current_user.learned? word %>
	    | <%= t(:learned) %> 
	  <% end %>
	</li>
<% end %>
<%= will_paginate %>